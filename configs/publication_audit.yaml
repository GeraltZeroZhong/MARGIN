schema_version: stability.postlock_submission_audit.v1
status: RECORDED_AFTER_OUTCOME_OPENING_BEFORE_EXTENDED_AUDIT_EXECUTION
seed: 20260825

analysis_role: descriptive_postlock_submission_audit
outcomes_were_open_before_specification: true
changes_registered_gates: false
authorizes_routing: false

paths:
  project_root: ..
  stability_config: configs/stability.yaml
  external_validation_protocol: configs/external_validation.yaml
  structure_sensitivity_protocol: configs/structure_sensitivity.yaml
  output_dir: runs/stability/postlock_submission_audit

inference:
  bootstrap_replicates: 5000
  confidence_level: 0.95
  minimum_variants_per_subset_domain: 10

fireprot_measurement_audit:
  repeated_measurement_minimum: 2
  high_consistency_maximum_ddg_range_kcal_mol: 0.5
  high_consistency_requires_sign_agreement: true

structure_geometry_audit:
  alignment: global rigid-body fit over all matched C-alpha atoms
  local_backbone_neighborhood_angstrom: 10.0
  local_backbone_rmsd_definition: post-alignment N-CA-C-O displacement for residues selected by experimental C-alpha distance
  contact_cutoff_angstrom: 8.0
  contact_minimum_sequence_separation: 3
  ca_lddt_neighborhood_angstrom: 15.0
  ca_lddt_thresholds_angstrom: [0.5, 1.0, 2.0, 4.0]
  chain_break_cn_distance_angstrom: 2.0
  minimum_positions_for_within_domain_correlation: 5
